<?php
// /src/controllers/solleciti_controller.php

/**
 * Prepara i dati per la pagina dello scadenzario.
 */
function solleciti_controller_data($conn, $pageConfig) {
    return []; // La pagina è gestita da FullCalendar via AJAX
}
