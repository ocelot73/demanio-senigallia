<?php
// /src/controllers/importa_controller.php

/**
 * Prepara i dati per la pagina di importazione.
 * In questo caso non ci sono dati da caricare dal DB.
 */
function importa_controller_data($conn, $pageConfig) {
    return []; // La pagina è gestita principalmente da JS e dalla logica SSE in index.php
}
